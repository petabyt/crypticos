; In order to dynamically retrieve/set a certain cell in memory,
; we must include a kernel function in the program.
inc "../kernel/main.casm"
jmp a
inc "../kernel/get.casm"
a:

; ga = mem[4]
set ga GET
set gb 4
run k.get

; Print as digit
add gr 50
sub gr 2
prt gr

; Expect 4, the location of gb.
